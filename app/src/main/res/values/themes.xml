<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The only XML theme in the app: it exists so the window has a background before Compose
      draws its first frame. Everything visible after that is CubeClashTheme.
    -->
    <style name="Theme.CubeClash.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.CubeClash</item>
    </style>

    <style name="Theme.CubeClash" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/canvas</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
